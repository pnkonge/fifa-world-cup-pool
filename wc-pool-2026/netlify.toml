[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so direct URLs and refresh work on every route.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
